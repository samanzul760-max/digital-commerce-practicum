# 全量对齐数据与审核能力

功能: 成员数据钻取

  场景: [SB-D-08][SB-D-09] 管理员读取当前实训室成员概览
    假如 管理员已登录并有当前实训室的访问权限
    当 管理员请求成员数据概览
    那么 服务端只返回当前实训室学员的匿名标识、完成率、已评分活动数和平均得分
    并且 结果可以按完成率降序稳定排序

  场景: [SB-D-08][SB-Q-04] 学生不能读取成员数据概览
    假如 学生已登录当前实训室
    当 学生请求成员数据概览
    那么 服务端返回稳定的 403 错误码
    并且 响应不包含任何其他学员的数据
